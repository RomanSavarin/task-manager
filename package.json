{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "task manager test task",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "start-server": "nodemon app.js",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev && npm run build"
  },
  "author": "Roman Savarin",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongodb": "^3.5.7"
  }
}